# R2 远端存储桶同步改造方案

## 一、改造目标

本次改造以 Cloudflare R2 远端配置为唯一真源。

目标如下：

1. 首页每次进入时，必须先同步远端存储桶信息，再加载对象列表。
2. 首页点击刷新时，必须先同步远端存储桶信息，再刷新对象列表。
3. 首页支持直接切换当前存储桶，切换数据来源于远端桶列表。
4. 存储桶的新增、删除、修改必须直接调用远端接口。
5. 自定义域名配置必须直接读取和修改远端配置，不再使用本地单独维护。
6. 当前对象上传、下载、删除等操作继续沿用现有 S3 兼容链路。
7. CORS、生命周期、桶锁、临时访问凭证、REST 对象接口替换 S3 链路，后续作为“高级设置”开发，本次不做。

## 二、本次范围

### 1. 纳入本次开发

1. 凭证结构调整
2. 首页远端同步
3. 首页直接切桶
4. 远端存储桶管理
5. 远端自定义域名管理
6. r2.dev 域名开关管理

### 2. 不纳入本次开发

1. CORS 策略管理
2. 生命周期规则管理
3. 桶锁规则管理
4. 临时访问凭证管理
5. 用 REST 对象接口替换现有 S3 对象链路

## 三、凭证与配置模型

### 1. 本地持久化字段

本地仅保存凭证和当前状态，不保存每个桶的本地配置副本。

建议保存字段如下：

1. `account_id`
2. `access_key_id`
3. `secret_access_key`
4. `api_token`
5. `jurisdiction`
6. `currentBucket`

### 2. 运行时配置规则

1. `endpoint` 由 `account_id` 自动拼接生成：
   `https://{account_id}.r2.cloudflarestorage.com`
2. `region` 固定为 `auto`
3. `publicUrl` 不再由本地配置直接决定，而是在每次同步远端后动态生成

### 3. 当前桶公开地址解析规则

当前桶的公开访问基址按以下优先级确定：

1. 当前桶中第一个已启用的自定义域名
2. 当前桶已启用的 `r2.dev` 域名
3. S3 endpoint 形式地址

## 四、接口使用方案

## 1. 继续保留的对象操作方式

对象上传、下载、列对象、删除对象继续使用现有 S3 兼容 API：

1. `ListObjectsV2`
2. `GetObject`
3. `PutObject`
4. `DeleteObject`
5. `DeleteObjects`

原因：

1. 当前项目已经具备稳定对象操作链路
2. 本次需求重点是桶管理与域名管理
3. 最小化修改可降低回归风险

### 1.1 大文件上传规则

本次增加如下上传约束：

1. 超过 `300 MB` 的文件，只能使用 S3 兼容性 API 上载
2. 首页和设置页如涉及上传能力，超过 `300 MB` 时必须强制走现有 S3 上传链路
3. 本次即使后续补充 REST 对象接口，也不能替代超过 `300 MB` 文件的上传路径

## 2. 本次新增使用的 Cloudflare REST API

### 存储桶管理

1. `GET /accounts/{account_id}/r2/buckets`
2. `POST /accounts/{account_id}/r2/buckets`
3. `GET /accounts/{account_id}/r2/buckets/{bucket_name}`
4. `PATCH /accounts/{account_id}/r2/buckets/{bucket_name}`
5. `DELETE /accounts/{account_id}/r2/buckets/{bucket_name}`

### 自定义域名管理

1. `GET /accounts/{account_id}/r2/buckets/{bucket_name}/domains/custom`
2. `POST /accounts/{account_id}/r2/buckets/{bucket_name}/domains/custom`
3. `GET /accounts/{account_id}/r2/buckets/{bucket_name}/domains/custom/{domain}`
4. `PUT /accounts/{account_id}/r2/buckets/{bucket_name}/domains/custom/{domain}`
5. `DELETE /accounts/{account_id}/r2/buckets/{bucket_name}/domains/custom/{domain}`

### r2.dev 域名管理

1. `GET /accounts/{account_id}/r2/buckets/{bucket_name}/domains/managed`
2. `PUT /accounts/{account_id}/r2/buckets/{bucket_name}/domains/managed`

## 五、首页改造方案

### 1. 首页进入时流程

首页初始化时，按以下顺序执行：

1. 读取本地凭证与 `currentBucket`
2. 调用远端“列出存储桶”接口
3. 校验本地 `currentBucket` 是否仍然存在
4. 若存在，则继续拉取该桶详情、自定义域名列表、r2.dev 配置
5. 若不存在，则自动切换到远端列表中的第一个桶
6. 用同步后的当前桶配置重建 `storageService`
7. 再加载对象列表

### 2. 首页刷新流程

点击首页刷新按钮时，执行与首页进入时相同的同步流程，而不是仅刷新对象列表。

### 3. 首页切桶功能

首页工具栏增加“当前桶”下拉框：

1. 数据来源：远端桶列表
2. 用户切换桶时：
   - 更新本地 `currentBucket`
   - 同步新桶详情、自定义域名、r2.dev 配置
   - 重建 `storageService`
   - 刷新对象列表

### 4. 远端异常处理

1. 若列桶失败，则保持当前桶状态不变，并提示同步失败
2. 若当前桶不存在且远端无可用桶，则进入空桶状态
3. 若详情同步失败，则不执行对象列表刷新

## 六、设置页改造方案

### 1. 凭证设置区

设置页凭证区调整为以下字段：

1. `account_id`
2. `api_token`
3. `access_key_id`
4. `secret_access_key`
5. `jurisdiction`
6. `endpoint` 只读显示

说明：

1. `endpoint` 根据 `account_id` 自动计算
2. 不再允许单独手工输入 endpoint

### 2. 存储桶管理区

设置页的存储桶页面改成“远端视图”，不再作为本地桶配置页。

建议结构：

1. 远端桶列表
2. 当前桶详情
3. 当前桶存储类型设置
4. 当前桶自定义域名列表
5. 当前桶 r2.dev 开关

### 3. 页面加载策略

设置页打开后先执行：

1. 同步远端桶列表
2. 根据当前选中桶拉取桶详情
3. 拉取当前桶自定义域名列表
4. 拉取当前桶 r2.dev 配置

### 4. 存储桶操作

#### 新增存储桶

1. 调用远端创建接口
2. 创建成功后重新拉取桶列表
3. 自动切换到新桶并加载详情

#### 修改存储桶

本次仅支持修改远端允许修改的项目：

1. 存储类型 `storageClass`
2. 自定义域名配置
3. r2.dev 开关

说明：

Cloudflare 当前接口不支持直接重命名存储桶，因此桶名创建后只读。

#### 删除存储桶

删除逻辑：

1. 先确认删除
2. 先清空桶内对象
3. 再调用远端删除桶接口
4. 删除成功后刷新桶列表
5. 若删除的是当前桶，则自动切换到其他可用桶

## 七、自定义域名改造方案

### 1. 展示形式

一个桶可绑定多个自定义域名，因此前端应采用列表形式展示，而不是单输入框。

### 2. 支持操作

1. 查询域名列表
2. 新增绑定域名
3. 修改域名配置
4. 删除域名绑定

### 3. 域名字段

每个自定义域名至少包含以下信息：

1. `domain`
2. `zoneId`
3. `enabled`
4. `minTLS`
5. `ciphers`

### 4. 修改域名的约束

如果仅修改以下属性，则直接调用更新接口：

1. `enabled`
2. `zoneId`
3. `minTLS`
4. `ciphers`

如果需要修改域名字符串本身，则建议采用以下方式实现：

1. 新增新的域名绑定
2. 删除旧域名绑定

原因：

1. 接口路径使用 `{domain}` 作为标识
2. 直接视作“改名”会带来语义歧义和兼容风险

## 八、主进程改造点

主要改造文件：

1. `src/main/main.js`
2. `src/main/infrastructure/R2Client.js`

### 1. `src/main/main.js`

主要工作：

1. 调整凭证字段读取与保存方式
2. 自动生成 endpoint
3. 扩展 Cloudflare REST API 调用封装
4. 增加以下 IPC：
   - 获取远端桶列表
   - 获取单桶详情
   - 获取单桶自定义域名列表
   - 绑定自定义域名
   - 修改自定义域名
   - 删除自定义域名
   - 获取 r2.dev 配置
   - 修改 r2.dev 配置
   - 同步当前桶完整配置
5. 切桶后重建 `storageService`

### 2. `src/main/infrastructure/R2Client.js`

主要工作：

1. 保持现有 S3 对象操作逻辑不变
2. 保持 URL 生成逻辑结构不变
3. 仅调整 `publicUrl` 来源为“远端同步后的当前桶配置”

## 九、前端改造点

### 1. 首页

涉及文件：

1. `src/renderer/index.html`
2. `src/renderer/renderer.js`
3. `src/renderer/styles.css`

主要工作：

1. 增加首页桶切换下拉框
2. 页面初始化时执行远端同步
3. 刷新按钮先同步远端再刷新对象
4. 切桶后刷新当前桶对象列表

### 2. 设置页

涉及文件：

1. `src/renderer/settings.html`
2. `src/renderer/settings.js`

主要工作：

1. 凭证表单重构
2. 删除“本地桶配置”概念
3. 引入远端桶列表和桶详情展示
4. 引入自定义域名列表 CRUD
5. 引入 r2.dev 开关设置

## 十、关键限制

### 1. 桶名不能直接编辑

根据当前接口文档：

1. 存储桶支持创建
2. 存储桶支持删除
3. 存储桶支持修改存储类型
4. 不支持直接重命名

因此本次实现中：

1. 创建时允许填写桶名
2. 已创建桶的桶名只读

### 2. 高级设置延期

以下功能不在本次开发中实现：

1. CORS
2. 生命周期规则
3. 桶锁
4. 临时访问凭证
5. REST 对象操作替换 S3

后续可统一放入“高级设置”分组。

### 3. 大文件上传限制

1. 超过 `300 MB` 的文件上传，必须使用 S3 兼容性 API
2. 该限制属于本次改造的明确约束，后续开发也需保持兼容

## 十一、实施顺序建议

建议按以下顺序实施：

1. 调整本地凭证结构与 endpoint 自动生成
2. 主进程补齐远端桶管理与域名管理接口
3. 实现“同步当前桶完整配置”的主进程能力
4. 改首页远端同步与切桶
5. 改设置页远端桶管理
6. 改设置页自定义域名管理
7. 联调对象列表、上传、下载、复制链接

## 十一、开发进度

1. [已完成] 调整本地凭证结构与 endpoint 自动生成
2. [已完成] 主进程补齐远端桶管理与域名管理接口
3. [已完成] 实现当前桶远端同步并重建 storageService
4. [已完成] 改造首页远端同步、刷新与直接切桶
5. [已完成] 改造设置页凭证表单与远端桶管理
6. [已完成] 改造设置页自定义域名与 r2.dev 管理
7. [进行中] 联调对象列表、上传、下载、复制链接

## 十二、结论

本次改造核心原则如下：

1. 远端配置为唯一真源
2. 首页进入和刷新都必须先同步远端
3. 首页直接切桶
4. 桶配置、域名配置都与远端保持一致
5. 对象操作继续沿用现有 S3 链路
6. 高级设置后续再做

## 本次收口完成项
1. [已完成] 首页存储桶切换样式收口与远端加载提示
2. [已完成] 设置页远端同步、切桶、桶/域名操作加载提示
